# Fyr Browser Python

This example pairs Fyr's reactive UI with Pyodide. Serve the folder over HTTP, then run the sample code. The first run downloads the Python runtime, so it requires an internet connection.
