# Fyr Rust / WebAssembly

Serve this folder through HTTP and click **Load engine**. It uses `WasmLoader` to fetch, compile, and inspect Fyr's published Rust engine. The package CDN URL is used instead of keeping a duplicate binary in this example.
